import styled from 'styled-components';
import {
  space,
  grid,
  boxShadow,
  gridTemplateRows,
} from 'styled-system';

export const Box = styled('div')(
  space,
  grid,
  boxShadow,
  gridTemplateRows
);
